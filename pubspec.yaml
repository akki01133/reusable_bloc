name: reusable_bloc
description: A generic bloc to fetch(and/or refetch) network data easily.
version: 1.0.3
repository: https://github.com/akki01133/reusable_bloc.git

environment:
  sdk: ^3.0.5

# Add regular dependencies here.
dependencies:
  bloc: ^8.1.2
  dartz: ^0.10.1
  equatable: ^2.0.5
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
